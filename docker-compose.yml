services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/app/data
    environment:
      - DB_PATH=/app/data/messages.db

volumes:
  bot-data:
